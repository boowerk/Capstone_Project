---
memoc: true
type: state
scope: project-memory
created: 2026-05-21T07:03:24
updated: 2026-06-08T00:00:00+09:00
status: active
tags:
  - memoc
  - memoc/state
---
# Current Project State

Last synced: 2026-06-12T00:00:00+09:00

## Current Status

- Do not run UBT/build while the editor is open. User prefers Live Coding for C++ changes.
- Multiplayer player movement fix in progress: `AGP_PlayerController` sends local `CurrentMoveInput` to the server, clears it reliably, and now resolves server-side effective move input from `CharacterMovement->GetCurrentAcceleration()` first. Directional target speed recalculates every tick; multiplayer assigns `MaxWalkSpeed` directly while standalone keeps speed interpolation. This targets client correction jitter and wrong remote facing.
- Action inertia is now standalone-only for direct movement: `AGP_PlayerCharacter` skips/clears `UpdateActionCarryVelocity`, `UpdatePrimaryAttackMovementAssist`, and `ApplyCurrentActionInertia` in multiplayer because their Tick-time `SafeMoveUpdatedComponent` / sampled velocity handoff can bypass CharacterMovement prediction and cause correction jitter.
- `UEFNSourceMesh` is the source animation mesh. `CharacterMesh0`/MaskMan should stay parented under `UEFNSourceMesh`; avoid reintroducing mesh Z hacks or detached retarget experiments.
- `ABP_UEFNSource_Player` uses motion matching with chooser-selected pose-search DBs, and `UGP_CharacterAnimInstance` owns locomotion context (`MovementMode`, `Stance`, `MovementState`, `Gait`, start/pivot/stop/TIP flags, graph DB state).
- Primary melee currently embeds recovery inside attack montages, blocks sprint while active, forces crouch during the combo, and restores uncrouch only if crouch input is no longer held.
- Skill VFX should be data-driven: montages keep timing events, abilities interpret cues, and `UGP_SkillData.VisualCues` owns Actor/Niagara selection. `UGP_SkillBase` now falls back to `DefaultSkillData` when the spec has no SkillData SourceObject.
- Primary Sword_Light VFX resolves trail/burst Niagara from SkillData cue tags and attaches to visible `GetMesh()` socket `hand_r`; direct Niagara notifies were removed from Sword_Light montages. `GA_Primary.DefaultSkillData` is now set to `/Game/GAS_Pattern/AbilitySystem/SkillData/DA_Skill_Primary`; VisualCues[0]=trail and [1]=burst with CueTags assigned. `UGP_Primary` still hard-fallbacks to that asset as a safety path.
- GameplayCue VFX tags are native-declared in `GP_Tags.h/.cpp` under `GPTags::GameplayCue::Ability` while keeping actual tag names `GameplayCue.Ability.Trail.Magic` and `GameplayCue.Ability.Burst.Magic`; config-only declarations were removed.
- Matador boss gameplay path exists in C++: `AGP_MatadorMageBossCharacter`, `UGP_MatadorBossStateComponent`, decoy/chain/bull actors, bull/groggy abilities, guarded/groggy tags, and DamageExec 10% guarded multiplier. Latest changes pass SkillData into common enemy/boss damage application, give bull charge explicit SetByCaller base/toughness damage, make DamageExec query target ASC tags directly, wire `BP_Boss_Matador` to `BT_BossCommon`/`BB_BossCommon`, stop bull repeated overlap damage, set decoy collision away from Pawn, and temporarily bias Matador tactics toward range keeping/area/bull instead of generic melee.
- Idle-start primaries stay full-body for the whole combo; moving-start primaries keep lower-body MM except configured combo indices, with moving lower-body blend alpha currently tuned to `0.65`.
- Current crouch/MM work depends on `CHT_MM_MaskMan_Root_OriginalStyle`, `ApplyChosenDatabase`, and the `ABP_UEFNSource_Player` MotionMatching -> PoseHistory -> LocomotionPose path using `RuntimePoseSearchDatabase`.
- Root-motion extraction tuning is in progress in `UGP_RootMotionExtractionEditorLibrary`, including snapped direction (`15` degrees) and foot-plant max-speed scale (`0.85`).
- Camera composition currently targets spring arm lengths `340/380/460` for idle/normal/sprint with socket offset `(0,65,20)`.
- Fab fence segment blueprint exists at `/Game/Fab/Blueprints/BP_Fab_FenceSegment`, assembled from `Baluster`, `Handrail`, and `NewelPost` to match current level `NewFolder1`: origin is the left post, segment runs along +Y to 460cm, has 10 balusters and 8 native handrail pieces.
- EventMap2 arena boundary: 48 generated StaticMeshActors labeled `ArenaFence_000` through `ArenaFence_047` use `/Game/Fab/Model/Fence.Fence`, placed on radius `5320.311680uu` around the origin. Actor scale is `(2.16,2.16,2.16)` to close visual gaps between fence posts; count is divisible by 4 for `+X/+Y/-X/-Y` symmetry. Rotation is Z yaw tangent-aligned.
- EventMap2 arcade ring: 48 generated arch bundles / 144 StaticMeshActors tagged `ArcadeRingGenerated` match the 48 fence segments and sit outside the fence at radius `5729.577951uu`, about `409.27uu` outside the fence radius `5320.311680uu`. Actor scale is `(2.5,2.5,2.5)`, so effective P2/P3 arch span is `750uu` and arc spacing exactly matches `750uu`. The whole arcade ring is rotated `+3.75` degrees (`180/48`) relative to the fence because fence pivots are mesh centers but arcade pivots are pillar positions. `ArcadeRing_Arch_NNN` uses `/Game/Fab/Model/Arcade_1F_Arch_P2` and `ArcadeRing_Base_NNN` uses `/Game/Fab/Model/Arcade_1F_Arch_P3`; their pivot location is the pillar location, and local `-Y` is chord-aligned toward the previous pillar (`ArchYaw = PillarYaw - 3.75`). `ArcadeRing_Pillar_NNN` uses `/Game/Fab/Model/Arcade_1F_Arch_P1`, shares the same location as its Arch/Base bundle, and uses radial/concentric yaw. All generated arcade actor `Location.Z` values are `0`.
- EventMap2 roof ring: 48 generated StaticMeshActors labeled `ArcadeRing_Roof_000` through `ArcadeRing_Roof_047` use `/Game/Fab/Model/Roof.Roof`. Each roof was spawned from the current matching `ArcadeRing_Arch_NNN` transform, so location, rotation, and scale exactly match the arch actor. Verification sample after placement showed `ArcadeRing_Roof_000` at `(5717.310,374.732,-400.000)`, yaw `0`, scale `(2.5,2.5,2.5)`, and no transform mismatches across all 48 pairs.
- EventMap2 pillar-top stone ring intentionally keeps 16 StaticMeshActors labeled `ArcadeRing_Stone_001`, `004`, `007`, ... `046` using `/Game/Fab/Model/Fence_Stone_Auditorium.Fence_Stone_Auditorium`. The user reduced the count deliberately; do not auto-restore to 48. Each remaining stone uses scale `(2.5,2.5,2.5)`, keeps its existing Z, shares its matching pillar yaw, and currently sits at radius `5854.577951uu` after being moved an additional `80uu` horizontally outward from the prior radius. Verification after move: 16 stones, labels `001/004/.../046`, radius min/max both `5854.578`.
- EventMap2 outer arcade ring exists under `PLAZA_DE_TOROS/Arcade_Outer`: 48 generated bundles / 144 StaticMeshActors labeled `ArcadeRing_Outer_Pillar_NNN`, `ArcadeRing_Outer_Arch_NNN`, and `ArcadeRing_Outer_Base_NNN`. It is concentric with the first-floor arcade at radius `6529.577951uu`, exactly `+800uu` from the first-floor arcade radius `5729.577951uu`. Each outer actor matches the corresponding first-floor arcade actor Z and rotation. Pillars keep scale `(2.5,2.5,2.5)`. Arches and bases stretch only local Y to scale `(2.5,2.849066,2.5)` based on the chord ratio `854.109661 / 749.464654 = 1.139626`. Verification after placement: 48 pillars, 48 arches, 48 bases, no Z/rotation/radius mismatches.
- EventMap2 second farther outer arcade ring exists under `PLAZA_DE_TOROS/Arcade_Outer2`: 48 generated bundles / 144 StaticMeshActors labeled `ArcadeRing_Outer2_Pillar_NNN`, `ArcadeRing_Outer2_Arch_NNN`, and `ArcadeRing_Outer2_Base_NNN`. It is concentric with the other arcade rings at radius `7329.577951uu`, exactly `+800uu` from `Arcade_Outer`. Each actor matches the corresponding `Arcade_Outer` Z and rotation. Pillars keep scale `(2.5,2.5,2.5)`. Arches and bases stretch only local Y to scale `(2.5,3.198132,2.5)` based on the chord ratio `958.754668 / 854.109661 = 1.122519`. Verification after placement: 48 pillars, 48 arches, 48 bases, no Z/rotation/radius mismatches.
- EventMap2 explicit high arcade ring exists under `PLAZA_DE_TOROS/Arcade_Outer3`: 48 generated bundles / 144 StaticMeshActors labeled `ArcadeRing_Outer3_Pillar_NNN`, `ArcadeRing_Outer3_Arch_NNN`, and `ArcadeRing_Outer3_Base_NNN`. It uses the user-requested radius `8055.0uu` and Z `2960.0uu`. Each actor uses the corresponding first-floor arcade angle/rotation. Unlike lower outer rings, all P1/P2/P3 actors now use uniform scale `(3.514657,3.514657,3.514657)`, derived from the high-ring chord ratio `1053.644412 / 749.464654 = 1.405863`, so pillars are enlarged too and arches/bases are not only stretched horizontally. Verification after scale update: 48 pillars, 48 arches, 48 bases, no radius/Z/scale mismatches.
- The high `ArcadeRing_Outer3` arcade ring now uses the 2F mesh family while preserving transforms and scale: Pillar actors use `/Game/Fab/Model/Arcade_2F_Arch_P1.Arcade_2F_Arch_P1`, Arch actors use `/Game/Fab/Model/Arcade_2F_Arch_P2.Arcade_2F_Arch_P2`, and Base actors use `/Game/Fab/Model/Arcade_2F_Arch_P3.Arcade_2F_Arch_P3`. The swap script updated 144 actors with no missing actors reported. Sample `000` actors kept radius `8055.000`, Z `2960.000`, and uniform scale `(3.514657,3.514657,3.514657)`.
- EventMap2 high arcade roof ring exists under `PLAZA_DE_TOROS/Arcade_Outer3_Roof`: 48 generated StaticMeshActors labeled `ArcadeRing_Outer3_Roof_NNN` using `/Game/Fab/Model/Roof.Roof`. Each roof copies matching `ArcadeRing_Outer3_Arch_NNN` XY and rotation, uses uniform scale `(3.514657,3.514657,3.514657)`, and has roof bounds bottom aligned to the matching high-arch bounds top. Placement sample after generation: index 000 at `(8037.754,526.822,3195.498)`, radius `8055.000`, yaw `0`, roof bottom `5500.207`, arch top `5500.207`; same contact check passed for 0/12/24/36. Full post-check was skipped because the editor entered Play mode.
- Generated plaza actors are organized under level folder `PLAZA_DE_TOROS`: fence actors in `PLAZA_DE_TOROS/Fence`, arcade actors in `PLAZA_DE_TOROS/Arcade`.
- World-aligned TerraCotta material instance exists at `/Game/Fab/WorldAlignedMaterials/MI_WA_TerraCotta`, parented to `/Game/Fab/M_WorldAlignedProjection`. Parameters: `BaseTexture=/Game/Textures/TerraCotta/Terracotta_albedo`, `NormalTexture=/Game/Textures/TerraCotta/Terracotta_normal`, `ORMTexture=/Game/Textures/TerraCotta/Terracotta_roughness`, `TileSize=300`, `ProjectionContrast=2`, `Roughness=1`.
- Base Slash VFX assets exist under `/Game/Niagara/Slash`: `M_BlazeSlashTip` is Additive/Unlit/Two Sided with Niagara Mesh Particles usage, and `NS_PlaySlash` has one active CPU emitter currently reported as `UpwardMeshBurst`. Current Niagara MCP should use value changes only, not add more duplicate modules. `User.Falloff` is set to 2.0, `MCP_UniformSize` is set to 2.0, Fixed Bounds are valid from `(-500,-500,-500)` to `(500,500,500)`, and `SM_PlaySlash` was not found; only `SM_BladeSlash` exists in the folder. MCP could not reliably rename the emitter to `MainSlash`, remove Scale Color, set `User.MCP_Color`, or prove exact Initialize Particle XYZ scale.
- `NS_PlaySlash` currently reports emitters `MainSlash`, `BlueVariation`, `PurpleVariation`, and `BlackVariation`; validation passes. Created `MI_BlazeSlash` and `MI_BlazeSlash_Trans`. MCP accepted emitter property payloads for requested material/color/offset/scale/dynamic parameter values, and user parameters now include `Distortion`, `DistortTextureVSpeed`, `DistortionAmount`, `MainTextureVSpeed`, `DissolveTextureVSpeed`, and `DissolveAmount`. MCP still reports the blue emitter as `BlueVariation` rather than `MainSlash_BlueVariation`, and cannot verify stack-level material override/dynamic parameter bindings.
- Slash distortion material groundwork: user imported image textures under `/Game/Niagara/Slash`. Disk assets include candidate textures `T_NoiseNormal_B`, `bigfire_single`, and `BezierCurve`. Intended material graph uses `DistortionSettings` Vector4 where R=SpeedX, G=SpeedY, B=MaskContrast, A=RefractionRate. SpeedX+SpeedY feed MakeFloat2 -> Panner.Speed, Panner feeds NormalTexture UV, NormalTexture RGB goes to Lerp.B, Constant3Vector `(0,0,1)` goes to Lerp.A, MaskTexture.R -> Power.Base, MaskContrast -> Power.Exp, Power -> Lerp.Alpha, Lerp -> Material.Normal, RefractionRate -> Material.Refraction, Constant 0 -> Material.Opacity. Attempted graph edit on 2026-06-09 failed because all Unreal MCP calls returned `Invalid or expired session ID`; material graph was not modified.
- Native projectile VFX replacement class `AGP_ProjectileMaster` exists in `Public/Actors/GP_ProjectileMaster.h` and `Private/Actors/GP_ProjectileMaster.cpp`. It creates `ProjectileCollisionBox`, `ProjectileVFXComponent` assigned `/Game/Niagara/Slash/NS_PlaySlash`, and `ProjectileVFXMovementComp` without hardcoded speed; gravity scale is 0 and UpdatedComponent is the collision box. It exposes `MuzzleVFX`, `HitImpactVFX`, `ProjectileDestroyDelay=0.5`, `MuzzleHitImpactSpawnOffset=ZeroVector`, implements offset-aware `SpawnVFX`, delays muzzle VFX by 0.1s, and on `ABlockingVolume` overlap stops movement, sets Niagara bool `User.KillMainSlash=true`, spawns hit VFX with offset, delays, then destroys. `/Game/Niagara/Slash/BP_ProjectileMaster` derives from `GP_ProjectileMaster`. The old scaffold was renamed to `/Game/Niagara/Slash/BP_ProjectileMaster_ScaffoldBackup`.
- Native spawner class `AGP_ProjectileVFXSpawn` exists in `Public/Actors/GP_ProjectileVFXSpawn.h` and `Private/Actors/GP_ProjectileVFXSpawn.cpp`. It exposes editable `MuzzleVFX`, `MainProjectileVFX`, `HitImpactVFX`, `ProjectileSpeed=800`, `ProjectileSpawnGap=2.0`, `ProjectileDestroyDelay=0.5`, `MuzzleHitImpactSpawnOffset=(0,0,100)`, and `SpawnedProjectile`. It defaults `ProjectileClass` to `/Game/Niagara/Slash/BP_ProjectileMaster` if found, defaults `MainProjectileVFX` to `NS_PlaySlash`, spawns once on BeginPlay, repeats by timer, injects VFX/delay/offset, and sets velocity to forward vector * ProjectileSpeed. `/Game/Niagara/Slash/BP_ProjectileVFXSpawn` now exists, derives from `GP_ProjectileVFXSpawn`, compiles/saves, and has expected CDO defaults.

## Open Tasks

- PIE validate crouch start/hold/release behavior, visible crouch locomotion, and running jump transition without waiting on run playback.
- Live Coding compile and editor-validate the latest extracted root-motion assets, especially foot-bone sourced extraction.
- Refresh any stale Blueprint pins after the latest animation / root-motion updates.
- PIE validate Primary Sword_Light trail/burst VFX from `DA_Skill_Primary`. See `Project_Eden/Docs/SkillVisualCueStructure.md`.
- Live Coding compile and PIE validate Matador boss: guarded takes 10% damage (`TargetGuarded=1`, `BossStateMult=0.10`), groggy takes full damage (`TargetGroggy=1`, `BossStateMult=1.00`), decoy forwards damage to boss ASC, bull hitting player deals damage + hit react, bull hitting decoy increments chain/groggy.
- Open `NS_PlaySlash` in Niagara editor and manually verify/adjust emitter name `MainSlash`, exact Initialize Particle non-uniform scale `(2.0, 2.0, 2.5)`, color intensity near 15/HDR, Dynamic Material Parameter binding for `Falloff`, Fixed Bounds `[-500,500]`, Cast Shadow off, and remove Scale Color / Color over Life if fade still repeats.
- Optional cleanup: delete `/Game/Niagara/Slash/BP_ProjectileMaster_ScaffoldBackup` after confirming the new native-parent `BP_ProjectileMaster` works in PIE.
- Manually verify `NS_PlaySlash` Kill setup: MCP added Bool `User.KillMainSlash` and set Kill Particles condition to `User.KillMainSlash == true`, and the system validates/saves. However, old Float `User.KillMainSlash` and Bool `User.MCP_KillParticlesEnabled` remain because MCP could not delete user parameters. Remove duplicate/unused params in Niagara editor if they cause ambiguity. Assign/create `NS_Muzzle` and `NS_HitImpact` if muzzle/impact bursts are needed.
- Manually inspect `NS_PlaySlash` Stack values for all variation emitters: rename `BlueVariation` to `MainSlash_BlueVariation` if desired, verify Blue material `MI_BlazeSlash`, Black material `MI_BlazeSlash_Trans`, Purple offset/color, Black offset/color/scale, and all Dynamic Material Parameters because MCP only reports broad success.
- Before retrying material graph work via MCP, reconnect/restart Unreal MCP. Inspect imported textures in editor: likely NormalTexture=`T_NoiseNormal_B`; decide whether `bigfire_single` or `BezierCurve` is the MaskTexture.
- Live Coding compile and PIE net-test player movement/facing; UBT from Codex failed before compile because local UE toolchain could not load `System.CodeDom`.
